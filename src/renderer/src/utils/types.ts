export enum Screens {
    landing = 'landing',
    main = 'main',
    colors = 'colors',
    icons = 'icons',
    backgrounds = 'backgrounds',
    languages = 'languages'
}

export type LanguageData = Record<string, Record<string, string>>
